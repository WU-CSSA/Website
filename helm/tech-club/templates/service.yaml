apiVersion: v1
kind: Service
metadata:
  name: {{ include "tech-club.fullname" . }}
  labels:
    {{- include "tech-club.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tech-club.selectorLabels" . | nindent 4 }}
