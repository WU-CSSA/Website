apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tech-club.fullname" . }}-config
  labels:
    {{- include "tech-club.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
