apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cssa.fullname" . }}-config
  labels:
    {{- include "cssa.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
